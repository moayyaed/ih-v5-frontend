import { main } from '../elements';
import move from '../elements/move';


const slider = {
  main: [
    { 
      title: 'Slider', 
      prop: 'slider', 
      type: 'divider',
    },
    ...main,
  ],
  move: move,
  link: [],
};


export default slider;
