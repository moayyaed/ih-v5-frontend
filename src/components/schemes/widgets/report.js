import { main } from '../elements';
import move from '../elements/move';


const report = {
  main: main,
  move: move,
  link: [{ type: 'actions2', prop: 'actions2', params: { variant: 'tree', id: 'reports' } }],
};


export default report;