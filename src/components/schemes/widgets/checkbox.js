import { main } from '../elements';
import move from '../elements/move';


const slider = {
  main: main,
  element: [
    { 
      title: 'Checkbox', 
      prop: 'checkbox', 
      type: 'divider',
    },
  ],
  move: move,
  link: [],
};


export default slider;
